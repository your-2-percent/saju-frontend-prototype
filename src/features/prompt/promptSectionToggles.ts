// features/prompt/promptSectionToggles.ts

// 프롬프트 출력에 포함할 섹션 토글(싱글/멀티 공통)
export type PromptSectionToggles = {
  tenGod?: boolean; // 십신 강약
  twelveUnseong?: boolean; // 십이운성
  twelveShinsal?: boolean; // 십이신살
  shinsal?: boolean; // 기타신살(신살)
  nabeum?: boolean; // 납음오행
};
